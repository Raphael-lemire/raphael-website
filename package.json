{
  "scripts": {
    "build": "mkdir -p dist/assets && cp index.html app.js styles.css favicon.ico robots.txt sitemap.xml apple-touch-icon.png site.webmanifest dist/ && cat app.js booking-calendar-polish.js > dist/app.js && cp -R assets/. dist/assets/ && cp -R public/. dist/",
    "check": "node --check app.js"
  }
}
